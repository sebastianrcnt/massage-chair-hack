import SwiftUI

extension Color {
    /// Primary CTA + nav tint. Soft indigo, calmer than systemBlue.
    static let chairTint = Color(red: 91 / 255, green: 91 / 255, blue: 214 / 255)
    /// Toggle "on" / active accent. Warm amber.
    static let chairActive = Color(red: 245 / 255, green: 158 / 255, blue: 11 / 255)
}
