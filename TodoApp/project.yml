name: TodoApp
options:
  bundleIdPrefix: com.siwonjeong
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    MARKETING_VERSION: 1.0
    CURRENT_PROJECT_VERSION: 1
    SWIFT_VERSION: 5.0
targets:
  TodoApp:
    type: application
    platform: iOS
    sources:
      - TodoApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.siwonjeong.TodoApp
        DEVELOPMENT_TEAM: CYYK3A72BM
        INFOPLIST_FILE: TodoApp/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_STYLE: Automatic
